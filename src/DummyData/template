	private static void template() {
		try {
			conn = util.open();
			for (int i = 0; i < 20; i++) {
				String sql = "INSERT INTO () VALUES()";
				pstat = conn.prepareStatement(sql);
				
				pstat.execute();
				pstat.close();
				
			}
			
			conn.close();
		} catch (Exception e) {
			e.printStackTrace();
		}
	}